{
  "devDependencies": {
    "@commitlint/config-conventional": "^19.5.0",
    "commitlint": "^19.5.0"
  },
  "scripts": {
    "build": "jekyll build",  
    "test": "commitlint --from=HEAD~1 --to=HEAD"  
  }
}